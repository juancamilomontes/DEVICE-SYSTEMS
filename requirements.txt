# Dependencias principales de device_systems.
# El proyecto usa `uv` (ver pyproject.toml); este archivo es equivalente para
# quien prefiera instalar con pip:  pip install -r requirements.txt

# Núcleo
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
sqlalchemy>=2.0
alembic>=1.20.0
pydantic[email]>=2.0
python-dotenv>=1.2.3

# Seguridad
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1          # fijado: passlib 1.7.4 no es compatible con bcrypt 5.x
slowapi>=0.1.10        # rate limiting
python-multipart>=0.0.32  # formularios OAuth2 (login)
