# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Base de datos local de SQLite (no se versiona: cada quien genera la suya)
*.db

# Variables de entorno con secretos (NUNCA se versiona; usar .env.example)
.env
